<!DOCTYPE html>
<html>
  <head>
    <title>Sports?!?!?!?</title>
  </head>

  <body>
      <% @data_hash.each do |team, positions| %>
      <h1><a href ="/teams/<%= team %>">Team: <%= team %></a></h1>
      <% positions.each do |position, person| %>
      <p><%= position %>: <%= person %> </p>
      <% end %>
      <% end %>
  </body>
</html>
